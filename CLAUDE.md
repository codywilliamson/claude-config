<!--
  This file is payload, not repo instructions.

  Sync copies it to ~/.claude/CLAUDE.md, where the import resolves to
  ~/.claude/AGENTS.md — sync deploys that file alongside this one, so both
  ends stay in step. Claude Code reads CLAUDE.md and not AGENTS.md, which is
  why this pointer exists at all.

  AGENTS.md holds the preferences themselves so Codex and anything else that
  reads the AGENTS.md convention gets the same text from one source. Add
  Claude-only instructions below the import, never above it.

  Guidance for working ON this repo lives in .claude/CLAUDE.md instead.

  HTML comments are stripped before this reaches the model, so this note
  costs no context.
-->

@AGENTS.md
